/**
 * Stub. The real implementation lands in spec 02.
 *
 * It will search the Pexels API for each theme using PEXELS_API_KEY from
 * .env.local, print the candidates for review (preview link, author, page url,
 * license link), write them to samples-raw/candidates.json, and download only
 * the marked ones. The key is used here and nowhere else - the app never
 * touches it.
 */
console.error(
  "samples:fetch is not implemented yet. See spec/02-sample-library.md.",
);
process.exit(1);
